{
  "scripts": {
    "build": "webpack",
    "dev": "webpack --watch",
    "build:prod": "NODE_ENV=production webpack",
    "standardfix": "standard --fix",
    "version": "npm run build:prod && git add dist/napchart.min.js",
    "postversion": "git push && git push --tags && npm publish"
  },
  "main": "lib/index.js",
  "name": "napchart",
  "version": "3.0.2",
  "description": "HTML5 Canvas time-planning library",
  "devDependencies": {
    "buble": "^0.18.0",
    "buble-loader": "^0.4.1",
    "webpack": "^3.10.0"
  }
}
